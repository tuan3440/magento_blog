<?php
$hours = $block->getHourOption();
$slots = $block->getSlot();
?>
<select id="option" name="hour">
    <?php foreach ($hours as $hour): ?>
        <option value="<?= $hour['value']?>"><?= $hour['label'].' ('. $slots[$hour['value']].' slot)'?></option>
    <?php endforeach;?>
</select>

<style>

</style>
